# Root railway.toml — intentionally minimal.
# Each service uses its own config file (set via Config File Path in Railway dashboard):
#   API service    → railway.api.toml
#   Worker service → railway.worker.toml
#   Web service    → railway.web.toml

[build]
builder = "dockerfile"
