# Per-service config files handle all build/deploy settings.
# Set "Config File Path" per service in Railway dashboard:
#   API service    → railway.api.toml
#   Worker service → railway.worker.toml
#   Web service    → railway.web.toml
